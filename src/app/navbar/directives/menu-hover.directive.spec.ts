import { MenuHoverDirective } from './menu-hover.directive';

describe('MenuHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuHoverDirective(null);
    expect(directive).toBeTruthy();
  });
});
